---
import ContentContainer from "@components/common/ContentContainer.astro";
import type { CollectionEntry } from "astro:content";

interface Props {
  tags: CollectionEntry<"tags">[];
}

const { tags } = Astro.props;
---

{
  tags.length > 0 && (
    <ContentContainer>
      <ul>
        {tags.map((tag) => {
          return (
            <li>
              <a href={`/tags/${tag.id}`}>&num;{tag.id}</a>
              {` `}
            </li>
          );
        })}
      </ul>
    </ContentContainer>
  )
}

<style lang="scss">
  ul {
    list-style-type: none;
    padding-left: 0px;
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 10px;

    ul {
      padding: 5px 7px;
    }
  }
</style>
