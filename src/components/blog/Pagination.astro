---
import type { Page } from "astro";

interface Props {
  url: Page["url"];
}

const { url } = Astro.props;
---

<ul>
  {
    url?.first && (
      <li>
        <a href={url.first} aria-label="Go to first page">First</a>
      </li>
    )
  }
  {
    url?.prev && (
      <li>
        <a href={url.prev} aria-label="Go to previous page">
          &larr;
          <span>Previous</span>
        </a>
      </li>
    )
  }
  {
    url?.next && (
      <li>
        <a href={url.next} aria-label="Go to next page">
          <span>Next</span>
          &rarr;
        </a>
      </li>
    )
  }
  {
    url?.last && (
      <li>
        <a href={url.last} aria-label="Go to last page">Last</a>
      </li>
    )
  }
</ul>

<style lang="scss">
  ul {
    list-style-type: none;
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    padding: 0;
    gap: 10px;

    li {
      width: 25%;
      min-width: 75px;

      a {
        display: block;
        background-color: var(--btn-primary-background);
        border-radius: var(--button-radius, 100px);
        color: var(--btn-primary-text);
        padding: 12px 18px;
        text-decoration: none;
        text-align: center;
        width: 100%;

        span {
          display: none;
        }

        &:hover {
          transform: scale(105%);
        }
      }
    }

    @media screen and (min-width: 415px) {
      flex-wrap: nowrap;
    }

    @media screen and (min-width: 475px) {
      gap: 20px;
    }

    @include screen("sm") {
      justify-content: center;
      gap: 30px;

      li {
        width: auto;
        a {
          span {
            display: inline-block;
          }
        }
      }
    }
  }
</style>
